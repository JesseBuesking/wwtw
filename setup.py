import os
from setuptools import setup

setup(
    name="wwtw",
    version="1.0",
    description="wibbly wobbly timey wimey - simple stopwatch with nice string formatting for python",
    packages=['wwtw', 'tests'])
